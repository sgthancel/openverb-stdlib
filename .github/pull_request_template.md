## What does this PR do?

A brief description of the change.

## Type of change

- [ ] New verb / verb family
- [ ] Bug fix (manifest, docs, or reference)
- [ ] Documentation update
- [ ] Reference implementation update
- [ ] Tooling / CI / config

## Checklist

- [ ] Manifests follow the standard format (`{ family, version, verbs: [...] }`)
- [ ] Input/output use JSON Schema types
- [ ] `npm test` passes (manifest validation)
- [ ] Documentation updated in `docs/website-starter-verbs.md` (if adding verbs)
- [ ] Reference implementations updated — all 3 (vanilla, React, Next.js)
- [ ] CHANGELOG.md updated

## Verb details (if adding a new verb)

**Verb ID:** e.g. `ui.example.verb`
**Family:** e.g. `ui.example`
**Level:** 0 / 1 / 2

## Example conversation flow

> If this PR adds or changes a verb, show how it works in a user-AI conversation.

1. User: "..."
2. AI calls `verb.id` with `{ ... }`
3. Result: `{ ... }`
